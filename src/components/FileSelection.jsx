export default function FileSelection() {
    return (
        <section id="data-selection">
            <span>Choose data file</span>
            <button id="upload-btn">
                <i id="upload-icon" className="fa-solid fa-arrow-up-from-bracket"></i>
                Upload File
            </button>
        </section>
    )
}